all:
  vars:
    ansible_python_interpreter: /usr/bin/python3

  children:

    master:
      hosts:
        master:
          ansible_user: mpiuser

    worker:
      hosts:
        node[1:3]:
          ansible_user: mpiuser